<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <title>게시판 - 상세보기</title>
    <style>
        body {
            font-family: sans-serif;
            max-width: 700px;
            margin: 40px auto;
            padding: 0 20px;
        }

        h1 {
            border-bottom: 2px solid #333;
            padding-bottom: 10px;
        }

        .info {
            display: flex;
            gap: 20px;
            color: #666;
            margin-bottom: 20px;
            padding-bottom: 10px;
            border-bottom: 1px solid #eee;
        }

        .content-area {
            min-height: 200px;
            padding: 20px;
            background: #fafafa;
            border-radius: 4px;
            white-space: pre-wrap;
            line-height: 1.6;
        }

        .image-area {
            margin-top: 20px;
        }

        .image-area img {
            max-width: 100%;
            border-radius: 4px;
            border: 1px solid #ddd;
        }

        .btn {
            padding: 8px 20px;
            border: none;
            border-radius: 4px;
            cursor:
                pointer;
            text-decoration: none;
            color: white;
        }

        .btn-secondary {
            background: #6c757d;
        }

        .btn-group {
            margin-top: 30px;
        }
    </style>
</head>

<body>
    <h1>{{board.title}}</h1>
    <div class="info">
        <span>작성자: {{board.writer}}</span>
        <span>조회수: {{board.viewCount}}</span>
        <span>작성일: {{board.createdAt}}</span>
    </div>
    <div class="content-area">{{board.content}}</div>
    <!-- 첨부 이미지 (파일이 있을 때만 표시) -->
    {{#board.fileName}}
    <div class="image-area">
        <p><strong>첨부파일:</strong> {{board.originalFileName}}</p>
        <img src="/images/{{board.fileName}}" alt="첨부 이미지">
    </div>
    {{/board.fileName}}
    <div class="btn-group">
        <a href="/board/list" class="btn btn-secondary">목록으로</a>
    </div>
</body>

</html>